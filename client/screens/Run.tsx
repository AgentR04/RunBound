import { StyleSheet, Text, View } from 'react-native';

const Run = () => {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      <Text>Run</Text>
    </View>
  );
};

export default Run;

const styles = StyleSheet.create({});
